name: CI

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8

      - name: Run flake8
        run: flake8 --ignore=E124,E128,E252,W293,E302,E303,E305,F401,E501,W503,E741 bot.py commands handlers utils

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Run pytest
        run: |
          set +e
          pytest -q
          code=$?
          set -e
          if [ "$code" -eq 5 ]; then
            echo "No tests were collected."
            exit 0
          fi
          exit "$code"

  security:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install bandit

      - name: Run bandit
        run: |
          bandit -r . --skip B311 -x .venv,venv,nextcord,utils/sql/get.py,utils/sql/convert_old_data.py,utils/settings/bot_ban.py,commands/bot/info.py
          bandit utils/sql/get.py --skip B311,B608
          bandit utils/sql/convert_old_data.py --skip B311,B608
          bandit utils/settings/bot_ban.py --skip B311,B608
          bandit commands/bot/info.py --skip B404,B603,B607
